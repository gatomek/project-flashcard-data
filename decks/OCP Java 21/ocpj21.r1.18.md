---
type: mcq
uuid: 13737132-9715-4ef8-b45b-4af0453d9954
---

# query

**R1.18.**
Which of the following statements about `var` is true?  
Choose all that apply.

## A

A `var` can be used as a constructor parameter.

## B

The type of a `var` is known at compile time.

## C

A `var` can not be used as an instance variable.

## D

A `var` can be used in a multiple variable assignment statement.

## E

The value of a `var` can not change at runtime.

## F

The type of a `var` can not change at runtime.

## G

The word `var` is a reserver word in Java.

# answer

## B

## C

## F
